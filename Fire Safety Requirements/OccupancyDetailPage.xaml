<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Fire_Safety_Requirements.OccupancyDetailPage"
             xmlns:viewmodel="clr-namespace:Fire_Safety_Requirements.ViewModel"
             x:DataType="viewmodel:OccupancyDetailViewModel"
             xmlns:controls="clr-namespace:Plugin.MauiMTAdmob.Controls;assembly=Plugin.MauiMTAdmob"
             xmlns:android="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.AndroidSpecific;assembly=Microsoft.Maui.Controls"
             Title="{Binding Text}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <!-- This will take up remaining space for WebView -->
            <RowDefinition Height="Auto" />
            <!-- This will automatically size for the Button -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- Scrollable content with WebView -->
        <ScrollView Grid.Row="0">
            <WebView Source="{Binding HtmlSource}" 
                     VerticalOptions="Fill"
                     android:WebView.EnableZoomControls="true"
                     android:WebView.DisplayZoomControls="true"/>
        </ScrollView>

        <!-- Back Button with correct positioning -->
        <Button Text="Close"
            Command="{Binding GoBackCommand}"
            Grid.Row="1"
            VerticalOptions="Start"
            HorizontalOptions="Center"
            Padding="10,5"
            MinimumHeightRequest="40"
                BackgroundColor="#c0392b"/>
        
        <controls:MTAdView BackgroundColor="Transparent"
                                           x:Name="myAds"
                                           AdsId="ca-app-pub-8158194714551266/7832271796"
                                           AdSize="AnchoredAdaptive"
                                           IsVisible="true"
                                            Grid.Row="2"/>
    </Grid>
</ContentPage>